    <%= form_with model: [ @post, @post.comments.build], local: false do |form| %>
    <p>
    <%= form.label :Name %><br>
    <%= form.text_field :commenter %>
    </p>
    <p>
    <%= form.label :Comment %><br>
    <%= form.text_area :body %>
    </p>
    <p>
    <%= form.submit id: 'hehe', class: 'sadas', data: { type: 'POST', url: post_comments_path(@post.id)} %>
    </p>
    <% end %>
